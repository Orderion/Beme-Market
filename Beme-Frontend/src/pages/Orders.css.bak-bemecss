/* src/pages/Orders.css — Minimal, matches site style */

/* ── Page shell ── */
.ord-page {
  min-height: 100vh;
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-main);
  padding: 36px 20px 72px;
}

.ord-wrap {
  max-width: 720px;
  margin: 0 auto;
}

/* ── Header ── */
.ord-head {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 28px;
  flex-wrap: wrap;
}

.ord-eyebrow {
  margin: 0 0 4px;
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--grtheme, #7c3aed);
}

.ord-title {
  margin: 0;
  font-size: 26px;
  font-weight: 900;
  letter-spacing: -0.04em;
  color: var(--text);
}

.ord-link-btn {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  height: 40px;
  padding: 0 18px;
  border: 1.5px solid rgba(0,0,0,0.14);
  border-radius: 100px;
  background: var(--card);
  color: var(--text);
  font-family: var(--font-main);
  font-size: 12px;
  font-weight: 700;
  text-decoration: none;
  transition: border-color 0.15s, box-shadow 0.15s;
  white-space: nowrap;
}
body.dark .ord-link-btn { border-color: rgba(255,255,255,0.12); }
.ord-link-btn:hover { border-color: var(--grtheme, #7c3aed); box-shadow: 0 0 0 3px rgba(124,58,237,0.1); }

.ord-action-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 44px;
  padding: 0 24px;
  border: none;
  border-radius: 100px;
  background: var(--grtheme, #7c3aed);
  color: #fff;
  font-family: var(--font-main);
  font-size: 13px;
  font-weight: 700;
  text-decoration: none;
  cursor: pointer;
  transition: opacity 0.15s;
}
.ord-action-btn:hover { opacity: 0.88; }

/* ── Empty / error card ── */
.ord-empty-card {
  background: var(--card);
  border: 1px solid rgba(0,0,0,0.08);
  border-radius: 16px;
  padding: 56px 28px;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10px;
  box-shadow: 0 2px 12px rgba(0,0,0,0.06);
}
body.dark .ord-empty-card { border-color: rgba(255,255,255,0.08); }

.ord-empty-icon {
  width: 56px; height: 56px;
  border-radius: 50%;
  background: var(--soft);
  border: 1px solid rgba(0,0,0,0.08);
  display: flex; align-items: center; justify-content: center;
  color: var(--muted); margin-bottom: 4px;
}
body.dark .ord-empty-icon { border-color: rgba(255,255,255,0.08); }
.ord-empty-icon--warn { color: #dc2626; background: rgba(220,38,38,0.06); border-color: rgba(220,38,38,0.15); }

.ord-empty-title { margin: 0; font-size: 18px; font-weight: 800; letter-spacing: -0.02em; color: var(--text); }
.ord-empty-text  { margin: 0; font-size: 13px; line-height: 1.65; color: var(--muted); max-width: 300px; }

/* ── Order list ── */
.ord-list { display: flex; flex-direction: column; gap: 20px; }

/* ── Order card ── */
.ord-card {
  background: var(--card);
  border: 1px solid rgba(0,0,0,0.08);
  border-radius: 16px;
  overflow: hidden;
  box-shadow: 0 2px 12px rgba(0,0,0,0.06);
  transition: background 0.25s, border-color 0.25s;
}
body.dark .ord-card { border-color: rgba(255,255,255,0.08); }

/* Card header */
.ord-card-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 16px 18px;
  border-bottom: 1px solid rgba(0,0,0,0.07);
}
body.dark .ord-card-header { border-bottom-color: rgba(255,255,255,0.07); }

.ord-card-header-left { display: flex; align-items: center; gap: 10px; }

.ord-card-icon {
  width: 38px; height: 38px; border-radius: 10px;
  background: var(--soft);
  border: 1px solid rgba(0,0,0,0.07);
  display: flex; align-items: center; justify-content: center;
  color: var(--text); flex-shrink: 0;
}
body.dark .ord-card-icon { border-color: rgba(255,255,255,0.07); }

.ord-card-label { margin: 0 0 2px; font-size: 10px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--muted); }
.ord-card-id    { margin: 0; font-size: 15px; font-weight: 900; letter-spacing: -0.02em; color: var(--text); font-family: monospace; }

/* Status badges */
.ord-badge {
  display: inline-flex; align-items: center;
  padding: 4px 11px; border-radius: 100px;
  font-size: 11px; font-weight: 700;
  text-transform: capitalize; white-space: nowrap;
  font-family: var(--font-main);
}
.ord-badge--green { background: rgba(34,197,94,0.1);  color: #15803d; }
.ord-badge--red   { background: rgba(239,68,68,0.1);  color: #b91c1c; }
.ord-badge--gray  { background: var(--soft);           color: var(--muted); }

/* Sections */
.ord-section { padding: 16px 18px; border-bottom: 1px solid rgba(0,0,0,0.07); }
.ord-section:last-of-type { border-bottom: none; }
body.dark .ord-section { border-bottom-color: rgba(255,255,255,0.07); }

.ord-section-head {
  display: flex; align-items: baseline;
  justify-content: space-between; margin-bottom: 14px;
}
.ord-section-title { font-size: 12px; font-weight: 800; text-transform: uppercase; letter-spacing: 0.06em; color: var(--text); }
.ord-section-sub   { font-size: 11px; color: var(--muted); font-weight: 600; }

/* ══════════════════════════════════════════════
   SELLER PROGRESS STEPPER
══════════════════════════════════════════════ */
.ord-progress { display: flex; flex-direction: column; }

.ord-p-step { display: flex; align-items: flex-start; gap: 12px; }

.ord-p-col {
  display: flex; flex-direction: column;
  align-items: center; flex-shrink: 0; width: 20px;
}

.ord-p-dot {
  width: 20px; height: 20px; border-radius: 50%; flex-shrink: 0;
  border: 2px solid var(--border, #E5E7EB);
  background: var(--card);
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s; z-index: 1;
}
.ord-p-dot--done   { background: #22C55E; border-color: #22C55E; }
.ord-p-dot--active { border-color: var(--grtheme, #7c3aed); border-width: 2.5px; }

.ord-p-line {
  width: 2px; flex: 1; min-height: 20px;
  background: rgba(0,0,0,0.09); margin: 3px 0;
}
body.dark .ord-p-line { background: rgba(255,255,255,0.09); }
.ord-p-line--done { background: #22C55E; }

.ord-p-body { flex: 1; min-width: 0; padding: 1px 0 18px; }
.ord-p-step:last-child .ord-p-body { padding-bottom: 0; }

.ord-p-name { font-size: 13px; font-weight: 700; color: var(--text); margin-bottom: 3px; }
.ord-p-step--active .ord-p-name { color: var(--grtheme, #7c3aed); }
.ord-p-sub { font-size: 11px; color: var(--muted); }
.ord-p-sub--done   { color: #16A34A; font-weight: 600; }
.ord-p-sub--active { color: var(--grtheme, #7c3aed); font-weight: 600; }

/* ══════════════════════════════════════════════
   DEFAULT STEPPER (fallback)
══════════════════════════════════════════════ */
.ord-default-track {
  display: flex; align-items: flex-start;
  overflow-x: auto; scrollbar-width: none; padding-bottom: 4px;
}
.ord-default-track::-webkit-scrollbar { display: none; }

.ord-dt-step {
  flex: 1; display: flex; flex-direction: column;
  align-items: center; gap: 6px; min-width: 60px;
}
.ord-dt-node { display: flex; align-items: center; width: 100%; }

.ord-dt-line { flex: 1; height: 2px; background: rgba(0,0,0,0.1); }
body.dark .ord-dt-line { background: rgba(255,255,255,0.1); }
.ord-dt-line--filled { background: var(--text); }

.ord-dt-dot {
  width: 20px; height: 20px; border-radius: 50%; flex-shrink: 0;
  border: 2px solid rgba(0,0,0,0.12); background: var(--card);
  display: flex; align-items: center; justify-content: center;
  z-index: 1;
}
body.dark .ord-dt-dot { border-color: rgba(255,255,255,0.12); }
.ord-dt-dot--done   { background: var(--grtheme, #7c3aed); border-color: var(--grtheme, #7c3aed); }
.ord-dt-dot--active { border-color: var(--text); border-width: 2.5px; }
.ord-dt-dot--future { border-color: rgba(0,0,0,0.1); }

.ord-dt-label { font-size: 10px; font-weight: 700; text-align: center; color: var(--text); white-space: nowrap; text-transform: uppercase; letter-spacing: 0.04em; }
.ord-dt-label--faint { color: var(--muted); }
.ord-dt-time  { font-size: 10px; color: var(--grtheme, #7c3aed); text-align: center; white-space: nowrap; font-weight: 700; }

/* ── Meta row ── */
.ord-meta-row {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
  border-bottom: 1px solid rgba(0,0,0,0.07);
}
body.dark .ord-meta-row { border-bottom-color: rgba(255,255,255,0.07); }

.ord-meta-item {
  padding: 12px 18px;
  border-right: 1px solid rgba(0,0,0,0.07);
}
body.dark .ord-meta-item { border-right-color: rgba(255,255,255,0.07); }
.ord-meta-item:last-child { border-right: none; }

.ord-meta-label { display: block; font-size: 10px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; color: var(--muted); margin-bottom: 4px; }
.ord-meta-value { display: block; font-size: 13px; font-weight: 700; color: var(--text); }
.ord-meta-truncate { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; max-width: 120px; }

/* ── Order summary ── */
.ord-summary {
  background: var(--soft);
  border: 1px solid rgba(0,0,0,0.07);
  border-radius: 10px; overflow: hidden;
}
body.dark .ord-summary { border-color: rgba(255,255,255,0.07); }

.ord-summary-row {
  display: flex; align-items: center; justify-content: space-between;
  gap: 12px; padding: 10px 14px;
  font-size: 12px; font-weight: 600; color: var(--muted);
  border-bottom: 1px solid rgba(0,0,0,0.06);
}
body.dark .ord-summary-row { border-bottom-color: rgba(255,255,255,0.06); }
.ord-summary-row:last-child { border-bottom: none; }
.ord-summary-row--sub      { padding-left: 26px; font-size: 11px; }
.ord-summary-row--discount { color: #16A34A; }
.ord-summary-row--total    { font-size: 14px; font-weight: 900; color: var(--text); }
.ord-summary-divider { height: 1px; background: rgba(0,0,0,0.09); }
body.dark .ord-summary-divider { background: rgba(255,255,255,0.09); }

/* ── Items ── */
.ord-items { display: flex; flex-direction: column; }

.ord-item {
  display: grid;
  grid-template-columns: 52px 1fr auto;
  gap: 12px; align-items: center;
  padding: 12px 0;
  border-bottom: 1px solid rgba(0,0,0,0.06);
}
body.dark .ord-item { border-bottom-color: rgba(255,255,255,0.06); }
.ord-item:last-child { border-bottom: none; }

.ord-item-img {
  width: 52px; height: 52px;
  border-radius: 10px;
  border: 1px solid rgba(0,0,0,0.08);
  overflow: hidden; background: var(--soft); flex-shrink: 0;
}
body.dark .ord-item-img { border-color: rgba(255,255,255,0.08); }
.ord-item-img-tag { width: 100%; height: 100%; object-fit: cover; display: block; }
.ord-item-img-empty { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: var(--muted); }

.ord-item-info { min-width: 0; }
.ord-item-name { display: block; font-size: 13px; font-weight: 700; color: var(--text); line-height: 1.4; }

.ord-item-chips { display: flex; flex-wrap: wrap; gap: 4px; margin-top: 5px; }
.ord-item-chip  {
  display: inline-flex; align-items: center;
  padding: 2px 8px; border-radius: 100px;
  border: 1px solid rgba(0,0,0,0.1);
  background: var(--card);
  font-size: 10px; font-weight: 600;
  color: var(--muted);
}
body.dark .ord-item-chip { border-color: rgba(255,255,255,0.1); }

.ord-item-right { text-align: right; flex-shrink: 0; }
.ord-item-price { display: block; font-size: 13px; font-weight: 800; color: var(--text); }
.ord-item-qty   { display: block; font-size: 11px; color: var(--muted); margin-top: 2px; }
.ord-more-items { margin: 10px 0 0; font-size: 12px; font-weight: 600; color: var(--muted); }

/* ── Card footer ── */
.ord-card-footer { padding: 14px 18px; display: flex; justify-content: center; }

.ord-view-btn {
  display: inline-flex; align-items: center; gap: 8px;
  width: 100%; justify-content: center;
  height: 44px;
  border: 1.5px solid rgba(0,0,0,0.12);
  border-radius: 100px;
  background: var(--card); color: var(--text);
  font-family: var(--font-main); font-size: 12px; font-weight: 700;
  text-decoration: none;
  transition: border-color 0.15s, box-shadow 0.15s;
}
body.dark .ord-view-btn { border-color: rgba(255,255,255,0.12); }
.ord-view-btn:hover { border-color: var(--grtheme, #7c3aed); box-shadow: 0 0 0 3px rgba(124,58,237,0.1); }

/* ── Skeleton ── */
.ord-skel {
  background: var(--soft);
  background-image: linear-gradient(90deg, var(--soft) 25%, var(--border) 50%, var(--soft) 75%);
  background-size: 600px 100%;
  animation: ord-shimmer 1.4s ease infinite;
}
@keyframes ord-shimmer {
  0%   { background-position: -600px 0; }
  100% { background-position: calc(600px + 100%) 0; }
}

/* ── Responsive ── */
@media (max-width: 640px) {
  .ord-page  { padding: 24px 14px 56px; }
  .ord-title { font-size: 22px; }
  .ord-head  { flex-direction: column; align-items: flex-start; gap: 12px; }
  .ord-link-btn { width: 100%; justify-content: center; }
  .ord-card-header { flex-wrap: wrap; }
  .ord-meta-row { grid-template-columns: 1fr 1fr; }
  .ord-meta-item { border-right: none; border-bottom: 1px solid rgba(0,0,0,0.07); }
  .ord-meta-item:nth-child(odd) { border-right: 1px solid rgba(0,0,0,0.07); }
  .ord-meta-item:last-child,
  .ord-meta-item:nth-last-child(2):nth-child(odd) { border-bottom: none; }
  .ord-item { grid-template-columns: 48px 1fr auto; }
  .ord-item-img { width: 48px; height: 48px; }
  .ord-action-btn { width: 100%; }
}